# ===== Required-ish (safe defaults) =====
N8N_TAG=latest
N8N_PORT=5678
GENERIC_TIMEZONE=Etc/UTC

# External URL (used by n8n for webhooks). Update to your domain or server IP later.
WEBHOOK_URL=http://localhost/

# Basic auth (protects n8n UI)
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=change-me-strong

# n8n encryption key (protects stored credentials) - generate a strong value!
# e.g. openssl rand -base64 48
N8N_ENCRYPTION_KEY=replace-with-super-long-random-string

# ===== Redis (internal only) =====
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=change-me-strong

# ===== Nginx / TLS =====
# Point your domain's A/AAAA record at this VM if using TLS.
DOMAIN=
USE_LETSENCRYPT=false
NGINX_EMAIL=
