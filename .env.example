# ===== n8n container =====
N8N_TAG=latest
N8N_PORT=5678
GENERIC_TIMEZONE=Etc/UTC

# External URL used by n8n for webhooks (set to your domain or VM IP)
WEBHOOK_URL=http://localhost/

# Basic auth to protect the n8n UI
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=change-me-strong

# Encrypts credentials stored by n8n (keep stable & secret)
# Generate: openssl rand -base64 48
N8N_ENCRYPTION_KEY=replace-with-super-long-random-string

# ===== Nginx / TLS =====
# Leave DOMAIN blank to run as default server on HTTP (local KVM)
DOMAIN=
USE_LETSENCRYPT=false
NGINX_EMAIL=
